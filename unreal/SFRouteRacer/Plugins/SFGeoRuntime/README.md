# SFGeoRuntime plugin placeholder

The runtime geospatial code currently lives as a game module under
`Source/SFGeoRuntime`. This directory is reserved if the module is later packaged
as a standalone plugin without changing include paths for gameplay code.
